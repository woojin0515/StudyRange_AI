# ADR (Architecture Decision Records)

This folder stores architectural decisions.

- Use sequential numbering (`0001`, `0002`, ...)
- Keep one decision per file
- Link related spikes for unresolved external dependencies
